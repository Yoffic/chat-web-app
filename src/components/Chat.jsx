import React from 'react';


const Chat = () => (
  <section className="col-9 d-flex px-4">
    {/* depends on channels buttons */}
    <section>Messages from participants</section>
    {/* looks the same */}
    <section className="mt-auto">
      Input message form
    </section>
  </section>
);

export default Chat;
